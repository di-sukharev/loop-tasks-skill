interface:
  display_name: "Loop Tasks"
  short_description: "Fresh subagent per task, full-task review"
  default_prompt: "Use $loop-tasks to complete the selected batch with a fresh implementing subagent per task, full-task review rounds, validation, commits, and pushes."
