interface:
  display_name: "Loop Tasks"
  short_description: "Efficient task queues with adaptive worker reuse"
  default_prompt: "Use $loop-tasks to complete the supplied task queue sequentially through subagents, with appropriate validation and independent review."
